```less
/*
  Tools Layer

  This layer can be composed by vendor tools (which don't style elements, objects or components),
  like the ones which only concern mixins, generators, functions and any other dynamic tool
  that a preprocessor like LESS accepts
*/
```
